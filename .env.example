# Optional local settings. QR login does not require a .env file.

# Optional: used by auth login --method password. Password mode can run
# headless and may fall back to QR if automatic login does not complete.
JACCOUNT_USERNAME=
JACCOUNT_PASSWORD=

# Optional: override runtime profile/artifact/log storage.
# JACCOUNT_HOME=/Users/admin/.jaccount-cli
